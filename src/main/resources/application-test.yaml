spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  apis:
    bitfrost:
      secret: test-secret
      tag: ExaGrad-Lecturer-Service
    auth:
      url: https://keycloak.sau-portal.de/realms/sau/protocol/openid-connect/token
      client-id: team-12
      client-secret: test-secret
